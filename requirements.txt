# AtnaTestFlow Dependencies

# Standard library modules used:
# - tkinter (GUI framework)
# - threading (background task execution)
# - os (file path operations)

# Development/Build dependencies:
PyInstaller>=5.0
Pillow>=9.0  # For icon creation (optional)

# Note: 
# - tkinter is included with Python on Windows and macOS
# - On Linux, install via: sudo apt-get install python3-tk (Debian/Ubuntu)
#                         or: sudo yum install python3-tkinter (RedHat/CentOS)